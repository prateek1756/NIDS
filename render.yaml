services:
  - type: web
    name: nids-backend
    env: python
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && python main.py
    envVars:
      - key: MONGODB_URL
        sync: false
      - key: DATABASE_NAME
        value: nids_db
